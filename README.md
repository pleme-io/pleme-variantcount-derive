# pleme-variantcount-derive

Enum-fold: pub const COUNT: usize = N + pub const fn count(). Any variant shape.

[![Build](https://github.com/pleme-io/pleme-variantcount-derive/actions/workflows/auto-release.yml/badge.svg)](#)
[![crates.io](https://img.shields.io/crates/v/pleme-variantcount-derive.svg)](https://crates.io/crates/pleme-variantcount-derive)

## Install

```toml
[dependencies]
pleme-variantcount-derive = "*"
```

## Generation

This crate is mechanically emitted by [`tatara-rust-ast`](https://github.com/pleme-io/tatara-rust-ast). The author surface is a typed `(defmacro …)` Spec — the proc-macro implementation, tests, Nix flake, caixa wrapper, and CI workflow are all generated. See the catalog at `catalog.json` in the parent registry.
